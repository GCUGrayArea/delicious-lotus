# API Configuration
# Base URL for the backend API
VITE_API_URL=http://localhost:8001
# Override the Vite dev proxy target if needed (defaults to localhost:8001)
VITE_API_PROXY_TARGET=http://localhost:8001

# WebSocket Configuration
# WebSocket URL for real-time updates (optional, defaults to derived from VITE_API_URL)
# VITE_WEBSOCKET_URL=ws://localhost:8001/api/v1/ws/jobs

# AI Generation Polling Configuration
# Polling interval in milliseconds for checking AI generation job status
# This is a fallback mechanism when WebSocket connections timeout
# Default: 5000 (5 seconds)
# Lower values = faster updates but more API calls
# Higher values = fewer API calls but slower updates
VITE_AI_POLLING_INTERVAL_MS=5000

# AI Generation Concurrency
# Maximum number of concurrent AI image/video generation jobs allowed
# Default: 30
VITE_MAX_AI_GENERATIONS=30
